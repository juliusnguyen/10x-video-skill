# Feature: VieNeu TTS Integration

## Goal
Replace the current TTS implementation (ITERA102) with the Python SDK `vieneu`.

## Context
- Current TTS script: `scripts/tts.sh`
- Target SDK: git@github.com:pnnbao97/VieNeu-TTS.git
- Project uses Python 3.12 in `.venv`.

## Requirements
1. Install `vieneu` SDK.
2. Update `scripts/tts.sh` (or create a new Python script) to use `vieneu`.
3. Ensure compatibility with the existing pipeline (Vietnamese support, output naming conventions).
