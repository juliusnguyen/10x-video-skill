Render the video slides to an MP4 file using Puppeteer.

Run: `node scripts/render-safe.js <N>`

Note: Before rendering, ensure you have updated the `videoNames` and `audioDurations` arrays in `scripts/render-safe.js` to match the current video.
