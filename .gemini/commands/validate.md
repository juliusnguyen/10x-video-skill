Validate HTML slides for syntax and runtime errors before rendering.

Run: `bash scripts/validate-html.sh video{N}_{name}`

This performs a Node.js syntax check and a Puppeteer runtime check. Both must pass before you proceed to rendering.
